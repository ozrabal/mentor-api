# Test Environment Configuration
# Copy this to .env.test and customize for your test environment

# Test Database
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mentor_test_db

# Application
NODE_ENV=test
PORT=0

# JWT (can use a simple test secret)
JWT_SECRET=test-jwt-secret-for-testing-min-32-chars

# Supabase (test/mock values - not needed for current phase)
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=test-anon-key
SUPABASE_SERVICE_ROLE_KEY=test-service-role-key

# Test Debug (set to true to see console output in tests)
TEST_DEBUG=false