/**
 * Health Module Public Contract
 *
 * This module's public API. Currently, health check doesn't need
 * to expose any ACL or events, but the structure is here for future use.
 */

// Health module doesn't expose any public contracts yet
// If other modules need to check health status, add ACL here

export {};
